## Task
refactor all the parameter construction in geometry scripts like `../../spider_generator/abdomen.py` to use the helper methods under `utilities.common`.
you can check `../../spider_generator/chelicerae.py` for reference since it's already refactored. it uses headings therefore inconsitent name and label, but this does not apply to other scripts.
